name: PII & Secrets Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  gitleaks:
    uses: fitz123/claude-code-bot/.github/workflows/gitleaks-reusable.yml@main
